info:
  name: Happy Path
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/v1/solve"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: "test(\"Status code is 200\", function () {\r

        \    expect(res.getStatus()).to.equal(200);\r

        });\r

        \r

        test(\"Response body is present\", function () {\r

        //     pm.response.to.have.body;\r

        });"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
